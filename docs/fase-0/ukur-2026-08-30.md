# Fase 0 — Hasil Pengukuran (Sandbox AgentDrop, HEAD 211920d)

> Status: DIUKUR KESEMPATAN. Yang tidak bisa diukur di sandbox ditandai ⚠.

## 1. Ukuran system prompt (proxy konteks per putaran)

| Berkas | Karakter | Baris | ≈ Token (4c/tok) |
|---|---|---|---|
| config/hermes/SOUL.md | 3.588 | 74 | ~900 |
| profiles/pekerja-quest/SOUL.md | 12.473 | 267 | ~3.100 |
| profiles/pekerja-harian/SOUL.md | 10.182 | 227 | ~2.550 |
| profiles/pekerja-koordinator/SOUL.md | 16.479 | 384 | ~4.100 |
| profiles/pekerja-x/SOUL.md | 10.902 | 238 | ~2.700 |
| profiles/pekerja-riset/SOUL.md | 9.683 | 219 | ~2.400 |
| profiles/pekerja-discord/SOUL.md | 9.229 | 200 | ~2.300 |
| profiles/pekerja-pantau/SOUL.md | 8.351 | 198 | ~2.100 |
| profiles/pekerja-daftar/SOUL.md | 10.874 | 231 | ~2.700 |
| skills/browser-operation/SKILL.md | 12.373 | 326 | ~3.100 |

**Total per profil + skill + snapshot 15.000 kar ≈ 20.000-25.000 kar ≈ 5.000-6.000 token per putaran.**

## 2. Audit turn counts (dari data/logs/)

⚠ **TIDAK TERUKUR** — `data/logs/` hanya berisi `.gitkeep` (kosong). Tidak ada log gateway atau `pre_tool_call` yang tersimpan dalam repositori ini. Angka "19 putaran" di dokumen berasal dari rekonstruksi alur berdasarkan `SOUL.md` + `SKILL.md`, bukan pengukuran live.

**Aksi yang diperlukan di mesin operator:** jalankan `agentdrop start`, lakukan 1 task `pekerja-quest`, lalu baca log gateway untuk menghitung `pre_tool_call` per task.

## 3. Chrome / CDP / chrome-extension

⚠ **TIDAK TERUKUR** — `curl http://127.0.0.1:9222/json/version` gagal (tidak ada respons). Binary Chrome tidak ditemukan di `/usr`; `lib/40-browser.sh` mengelola binari melalui `chrome-for-testing` yang diunduh saat `install.sh`. 

**Aksi yang diperlukan di mesin operator:** setelah `agentdrop browser` berjalan dengan wallet aktif, jalankan:
```bash
curl -fsS http://127.0.0.1:9222/json | grep chrome-extension | wc -l
```
Jika hasil > 0, Fase 3 (popup) bisa diuji. Jika 0, masalahnya struktural seperti yang dijelaskan (`browser_supervisor.py:745`).

## 4. Latensi endpoint per putaran (prefill vs generasi)

⚠ **TIDAK TERUKUR** — endpoint `api.hcnsec.cn` (atau endpoint yang dikonfigurasi di `.env`) tidak terjangkau dari sandbox ini (egress diblokir). Tidak mungkin mengukur waktu generasi.

**Implikasi:** Semua optimasi harga (batch, playbook) harus dinilai dari **jumlah putaran turun**, bukan dari perbaikan endpoint. Ini konsisten dengan rekomendasi doc: "mengecilkan putaran adalah satu-satunya tuas yang bekerja lepas dari endpoint mana pun".

## 5. Subprocess / IPC measurement (dari repo, bukan live)

✅ **TERUKUR DARI DOBUMENTASI REPO** (`docs/analisis-bottleneck.md`):
- Biaya per `Popen` + temp file + baca + parse = **1,7 ms** (replikasi persis langkah 5 di `tools/browser_tool.py`).
- 40 aksi = **0,07 s**.
- Kesimpulan: **bukan bottleneck**.

## 6. Plugin framework

⚠ **BELUM TERPASANG** — `ls -la ~/.hermes/plugins/` tidak ada; `find /usr/local/lib/agentdrop` kosong karena `install.sh` belum dijalankan di sandbox ini (repo hanya checkout). Plugin akan dipasang di `~/.hermes/plugins/agentdrop-browser/` saat Fase 2.

---
## Kesimpulan Fase 0

| Item | Status | Tindakan selanjutnya |
|---|---|---|
| Ukuran prompt | ✅ Diukur | Konfirmasi konteks membengkak valid |
| Turn counts live | ⚠ Tidak terukur | Operator harus jalankan task + baca log gateway |
| Chrome / CDP / extension | ⚠ Tidak terukur | Operator jalankan `agentdrop browser`, cek `/json` |
| Latensi endpoint | ⚠ Tidak terukur | Tidak bisa diubah dari sisi kita; fokus pada putaran |
| Subprocess overhead | ✅ Diukur (1,7 ms) | Tidak perlu ganti bahasa |
| Plugin framework | ⚠ Belum ada | Siapkan saat Fase 2 |

**Rekomendasi setelah Fase 0:** mulai **Fase 1** (revisi prompt/SOUL) tanpa menunggu hasil lengkap Fase 0, karena Fase 1 tidak bergantung pada angka latensi endpoint atau turn counts — ia hanya mengubah instruksi teks dan aturan batching.
