/*
 * config.local.js — DIGENERASI oleh scripts/setup.sh dari .env.
 *
 * Versi di repo ini sengaja KOSONG. Jangan isi manual kalau bisa dihindari;
 * jalankan ulang setup.sh supaya nilainya selalu sinkron dengan .env.
 *
 * File ini tidak boleh berisi private key. Hanya URL daemon dan token.
 */
/* eslint-disable no-var */
var AGENTDROP_CONFIG = {
  baseUrl: 'http://127.0.0.1:9721',
  token: '',
  timeoutMs: 60000
};
