# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/

# Run artifacts / evidence (ephemeral, regenerated by `agentdrop poc`)
runs/
poc/evidence/
# ...except the curated interactive demo evidence (a fixed artifact)
!poc/interactive/evidence/
poc/interactive/.state.json

# Browser profiles (large, contain cookies — never commit)
browser-profiles/
*.user-data-dir/

# Local env / secrets
.env
*.local
agentdrop.env

# OS
.DS_Store
